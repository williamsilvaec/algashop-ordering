spring:
  application:
    name: ordering
  datasource:
    url: jdbc:postgresql://localhost:5432/ordering_test
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    oa.open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/clean
    user: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/ordering_test

algashop:
  integrations:
    shipping.provider: FAKE
    rapidex.url: http://localhost:8780
    product-catalog.url: http://localhost:8781